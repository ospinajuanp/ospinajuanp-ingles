node_modules
dist
dist-ssr
*.local
.DS_Store
.vite
.next
.tsbuildinfo
tsconfig.tsbuildinfo
.env
.env.*
!.env.example

# Source JSON lives in public/ and is fetched at runtime.
# The root copy is a build artifact (regenerated by generar_estructura.py / llenar_verbos.py).
/verbos_estructura.json

# Tooling scripts that generate the canonical JSON (kept locally, tracked only if useful).
# generar_estructura.py
# llenar_verbos.py
